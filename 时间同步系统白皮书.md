# 多传感器时间同步系统白皮书

**适用系统：USB 相机 + IM948 IMU（串口）+ 步进电机（串口），Windows 平台**

**相机采集管线：FFmpeg/dshow 或 Media Foundation（含从 Micro-Manager 迁移的考量）**

版本：v1.1 · 日期：2026-06-12

---

## 1. 问题定义

系统包含三个异构数据/控制通路：

| 通路 | 接口 | 标称速率 | 自带时钟 | 时间戳现状 |
|------|------|----------|----------|------------|
| USB 相机 | USB (UVC) | ~30 fps | 无（依赖驱动打戳） | 后端相关：MM ElapsedTime / FFmpeg PTS / MF DeviceTimestamp |
| IM948 IMU | USB 转串口 | ~200 Hz | 板载晶振（几十 ppm 级） | 包内时间戳 / 包序号 |
| 步进电机 | USB 转串口 | 命令驱动 | 无 | PC 发令时刻 |

目标：将三路事件统一到单一时间轴上，使任意两路之间的相对时刻误差控制在毫秒级（目标 1–2 ms），且在长时间运行（小时级）下不发生漂移累积。

核心约束：IM948 无外部硬件同步（sync/trigger）引脚，相机为普通 UVC 设备无策略触发能力，因此本系统只能采用**软件域同步 + 物理事件标定**的架构，这也决定了精度上限在毫秒量级。

---

## 2. 时钟域分析

### 2.1 候选时钟与其缺陷

**PC walltime（系统时钟）**：受 NTP 调整影响，可能发生跳变甚至回退，绝对不可作为对齐运算的基准。其唯一职责是为数据集提供绝对日期标签。

**PC steady_clock / QPC**：单调递增、不受 NTP 影响、分辨率亚微秒级。Windows 下 `std::chrono::steady_clock` 底层即 QueryPerformanceCounter（QPC），DirectShow 参考时钟亦同源。这是全系统唯一合格的主时间轴。

**IM948 板载晶振**：精度约数十 ppm，每小时漂移可达上百毫秒，不可独立作为基准。更根本的问题是拓扑性的：相机与电机不在 IMU 时钟域内，最终仍须翻译到 PC 时钟，因此"以 IMU 为准"在架构上不成立。但晶振的**短期间隔稳定性远优于串口到达时间**，必须保留用于流内插值。

**串口到达时间**：到达时刻 = 真实采样时刻 + 传输延迟 + USB 转串口芯片缓冲 + OS 调度抖动。CH340/CP210x 类芯片抖动可达 1–16 ms，直接使用会将 IMU 的均匀采样打成锯齿。

### 2.2 架构结论

```
                    ┌────────────────────────────┐
   walltime ───────►│ 启动时记录一次 (walltime,   │
   (仅绝对标签)      │  steady_clock) 锚点         │
                    └────────────────────────────┘
                                 │
                    ╔════════════▼═══════════════╗
                    ║   主时间轴: steady_clock    ║
                    ║        (QPC 域)            ║
                    ╚════╦═══════╦═══════╦═══════╝
                         │       │       │
              线性拟合映射 │       │映射+固定偏移   │ 直接记录
                         │       │       │
                    IMU 晶振   相机 PTS   电机命令
                    (流内插值) (流内插值)  + 执行延迟标定
```

**单一主轴原则**：所有对齐运算只在 steady_clock 域进行。各传感器的本地时钟仅用于恢复各自流内的精细采样间隔，再通过时钟映射模型挂载到主轴。

---

## 3. IMU 通路设计

### 3.1 数据记录

每个 IMU 数据包记录二元组：

- `t_imu`：IMU 内部时间戳。优先使用 IM948 协议中的 timestamp 字段；若协议无此字段，以包序号 × 标称采样周期代替（需确认协议含递增序号以检测丢包）。
- `t_arrival`：该包在 PC 侧被读取时的 steady_clock 时刻（在串口读线程中尽早打戳）。

### 3.2 时钟映射模型

建立线性映射 `t_master = a · t_imu + b`，其中斜率 a 吸收晶振频偏（ppm 级漂移），截距 b 吸收固定传输延迟。

**拟合方法：下包络拟合（lower-envelope fit），优于普通最小二乘。** 原理：传输抖动只会单向增大延迟（包只会晚到不会早到），故每个时间窗内延迟最小的点最接近真实传输路径下界，其余散点皆为抖动污染。算法：

1. 将数据按时间分窗（如每 1 s 一窗）；
2. 每窗内取 `t_arrival − t_imu` 最小的样本点；
3. 对这些下包络点做线性回归（可加 RANSAC 提升鲁棒性）；
4. 长时间运行时采用滑动窗口在线更新 a、b，以跟踪温漂导致的频偏变化。

映射后，每个 IMU 样本的主轴时刻为 `a · t_imu + b`——既保留晶振级的间隔精度，又锚定于主时钟，串口抖动被滤除。

---

## 4. 相机通路设计（Windows 采集后端）

相机是全系统中时间戳语义最复杂、最易出错的通路。**核心认识：更换采集后端（MM → FFmpeg → MF）本身不会自动提升精度，它只改变"打戳点在流水线中的位置"以及"你能读到哪些时间戳字段"。真正决定精度的是相机驱动是否暴露硬件/设备时间戳，以及你是否读对了字段。** 选错字段，再现代的后端也只是把一个末端软件戳换成另一个末端软件戳。

### 4.1 时间戳打戳点的流水线位置

一帧图像从产生到被应用拿到，经历如下链路，每一环都可能是某个后端的打戳点：

```
曝光 → 传感器读出 → USB 等时传输 → 内核 UVC 驱动 → 用户态采集层缓冲 → 应用回调/取帧
  │         │                          │                    │              │
  └曝光中点  └读出完成                  └驱动设备戳(最佳)     └MMCore环形缓冲  └软件到达戳(最差)
                                        DeviceTimestamp/UVC PTS            ElapsedTime/wallclock
```

打戳点越靠左越接近真实采集时刻、抖动越小。各后端的可用字段正是落在这条链路的不同位置上。

### 4.2 Micro-Manager：为何其时间戳只到 ±20 ms

MM 的 `ElapsedTime-ms` 是相机实际曝光时刻的代理量，但其打戳点位于链路**最末端**——图像穿过 MMCore 环形缓冲、被 Java 层从 Core 取出之后才打戳。官方文档明确声明该字段抖动大，不应假定其精度优于约 20 ms；另一字段 `Time` 是 Java 层收到图像的时刻，与相机实际采集时刻之间存在显著且可变的延迟，且总是滞后。

工程后果：若相机跑在 100 Hz（10 ms/帧），±20 ms 即对应 ±2 帧的不确定度——这正是"IMU 对齐后视频差几帧"现象的直接来源。该偏移量级落在 MM 自己声明的不确定区间内，并非 IMU 出错。

**但 MM 内部可能藏有真正可信的戳**：若使用 Photometrics/Teledyne、Hamamatsu、Andor 等科研相机，开启相机适配器的 `MetadataEnabled` 后，metadata 中会出现由相机板载处理器生成的硬件时间戳（如 PVCAM 的 `PVCAM-FMD-TimestampBofNs`/`EofNs`，帧起始/结束纳秒）。这是绕过缓冲与 Java 层、直接来自相机的最精确时间戳。**结论：若被迫留在 MM，先查 Device Property Browser 是否有硬件戳；有则弃用 ElapsedTime 改用硬件戳，无则 ElapsedTime 仅 ±20 ms，须靠 IMU 做相对参考 + LED 标定固定偏移。**

### 4.3 迁移到 Media Foundation：读对字段是关键

MF 提供两个极易混淆的时间字段，选错则前功尽弃：

- **`IMFSample::GetSampleTime()`**：返回 **presentation time（呈现时间）**，单位 100ns。这是相对流开始的排程时间轴，供渲染管线使用，**不是采集时刻的绝对量，不要用于跨模态对齐**。当驱动未提供设备戳时，MF 会用系统时钟合成此值。
- **`MFSampleExtension_DeviceTimestamp` 属性**（经 `GetUINT64` 读取）：由采集设备的 media source 设置，其值位于 MFTIME 域，**与 QPC 共享同一 epoch，始终以 100ns 为单位**。这才是目标字段——直接落在 QPC 域、与 `steady_clock` 同源、零点无需换算的设备侧采集时刻。

若此字段可用，相机与 IMU 天然处于同一根尺子上，省去整层时钟映射。注意：实践中存在大量相机驱动不填该字段的情况（社区已反复报告 `OnReadSample` 回调的 `llTimestamp` 与 `GetSampleTime()` 均非硬件 PTS），此时读取会返回 `MF_E_ATTRIBUTENOTFOUND`。

### 4.4 dshow：与 MF 共享同一驱动，无字段优势

dshow 的 `IMediaSample::GetTime` 由 capture filter 基于 DirectShow 参考时钟（底层 QPC）在帧进入 filter 时打戳。它与 MF 走**同一个内核 UVC 驱动**，故若驱动不暴露硬件 PTS，dshow 与 MF 谁都变不出来。UVC 协议层本有 `dwPresentationTime` 与 source clock reference 字段，但能否透传到用户态完全取决于相机固件是否填写、以及驱动是否转译——许多消费级 webcam 固件根本不填。

**MF vs dshow 选型**：二者在"能否拿到硬件戳"上等价（取决于同一驱动）。若驱动给设备戳，两者皆可，优选 MF（API 更现代、异步回调模型更干净，且 `MFSampleExtension_DeviceTimestamp` 是直接的 QPC 域值）。若驱动不给，两者相对 MM 的唯一收益是打戳点前移（省去 MMCore 缓冲 + JNI），抖动可能从 ±20 ms 降至几 ms，但仍是软件戳。

### 4.5 驱动能力探针（迁移前必做，勿先重写框架）

**不要在判定驱动能力前重写整个采集框架。** 先写一个最小探针（MF SourceReader 异步回调即可），对每帧同时记录三个量并打印：

1. `GetSampleTime()`——presentation time（相对流起点）；
2. `GetUINT64(MFSampleExtension_DeviceTimestamp, ...)` 的 HRESULT 与值；
3. 回调内立即取的 QPC（`QueryPerformanceCounter`）。

判定逻辑：

| 探针结果 | 含义 | 迁移决策 |
|----------|------|----------|
| DeviceTimestamp 可用，且与回调 QPC 差为稳定小正数（几 ms） | 驱动提供 QPC 域采集戳 | **大赚**：直接用 DeviceTimestamp，相机与 IMU 零映射对齐，重写值得 |
| DeviceTimestamp 返回 `MF_E_ATTRIBUTENOTFOUND` | 驱动不给硬件戳 | **小赚**：MF/dshow 相对 MM 仅打戳点前移（±20ms→几 ms），仍为软件戳，仍需下包络拟合 + LED 标定；按工程量权衡是否值得迁移 |

### 4.6 时间戳质量验证（部署前必做）

无论使用哪个后端的哪个字段，录一段数据并同时记录每帧到达的 QPC，做两项检查：

- **时间戳间隔直方图**：30 fps 应呈尖锐集中于 33.3 ms 的单峰。若出现锯齿交替（如 17/50 ms）或宽弥散，说明该时间戳实为软件补打，质量不可信。
- **时间戳 − QPC 差值随时间曲线**：斜率非零表明存在跨时钟域漂移，须用线性拟合映射而非常数偏移。

（FFmpeg/dshow 路径补充：禁用 `use_wallclock_as_timestamps 1`（demuxer 读包时刻、经 rtbuffer 排队，最差）；保留默认 `use_video_device_timestamps=true`。但注意 FFmpeg 输出的 PTS 常被换算为"启动瞬间 walltime + QPC 增量"的混合值，增量可信、零点带一次 walltime 误差，须经映射而非直接使用。）

### 4.7 处理策略汇总

- **拿到 QPC 域设备戳（DeviceTimestamp / 相机硬件戳）**：直接作为相机采集时刻，与 IMU 同处 QPC 主轴，无需流内映射，仅需 4.8 的曝光中心偏移标定。
- **仅有软件到达戳（MM ElapsedTime / MF presentation time / dshow filter 戳）**：将其视为"相机本地时钟"，与 IMU 同法——对 (时间戳, 回调 QPC) 做下包络线性拟合映射至主轴，再叠加 LED 标定的固定偏移。

### 4.8 曝光中心偏移

无论时间戳来源如何，其语义均为帧的**采集/读出/到达时刻**之一，均非曝光中心。与曝光中心之间隔着：曝光时间/2 + 传感器读出时间 + USB 等时传输时间（USB 2.0 上一帧 MJPEG 传输本身需数毫秒至十余毫秒）。该项为"稳定固定偏移 + 小抖动"，由第 6 节物理事件标定一次性测出。若实验中曝光时间动态变化，须固定曝光或按曝光设置分段标定。

> **诊断专题：IMU 对齐后视频差几帧，该信谁？**
> 这不是"IMU vs 后端谁说谎"，而是两把尺子刻度语义不同。IMU 经下包络拟合后是更干净的**相对**尺子（间隔精度高），但其绝对零点仍靠启动锚点确定，并非绝对真值。诊断第一步是**判定偏移是恒定还是增长**：
> - **恒定偏移** → 相机通路固定延迟 + 两时钟域零点差，物理必然，无需"选信谁"，LED 标定减去常数即可。
> - **增长偏移** → 存在 ppm 级频率失配。画"IMU 绝对时刻 − 相机绝对时刻"对时间的直线，斜率即失配率，方向指向跑偏的时钟（常见为 IMU 拟合 a/b 过时未跟温漂，或相机绝对时间换算中混入了被 NTP 拽动的 walltime）。
>
> 另需排除语义陷阱：若用共同运动做互相关对齐，相机帧是在曝光窗口上**积分**的（质心落在曝光中点附近），本身就比 IMU 瞬时采样晚"曝光/2 + 读出"，这是拿积分量对瞬时量，非时钟错误。

---

## 5. 步进电机通路设计

电机无数据时间戳问题——命令由 PC 发出，发令时刻天然位于 steady_clock 域。真正的不确定量是**执行延迟**：串口传输 + 控制器固件解析 + 加减速起步时间。处理方式：

- 该延迟视为固定偏移，由第 6 节联合标定测定；
- 若需更高置信度，可单独标定：令电机触发限位开关 / 光电门并记录触发时刻，与发令时刻之差即执行延迟，多次测量取统计量并评估抖动幅度;
- 发令打戳应在 `WriteFile` / 串口写调用**之前与之后各取一次** QPC，以界定串口写阻塞带来的不确定区间。

---

## 6. 跨模态联合标定（系统级裁判）

设计一个三方同时可观测的物理事件，一次性测出各通路相对主轴的总固定偏移，并以残差验证全系统对齐精度。

### 6.1 推荐方案 A：LED 闪光（标定相机通路）

PC 经串口控制 LED 短闪。相机帧序列中检测亮度突变帧（亚帧精度可由前后帧亮度插值获得），比对"PTS 时间轴上的事件时刻"与"主时钟上的发令时刻"，所得差值打包涵盖了曝光偏移、传输延迟、时钟域零点差，无需逐项分解。LED 电路响应在微秒级，是最干净的相机标定源。

### 6.2 推荐方案 B：机械冲击（联合标定三通路）

步进电机驱动挡板快速敲击/越过某位置：

- 相机观测到挡板位置/亮度突变（某帧）；
- IMU 刚性固定于运动件，记录到加速度尖峰；
- 电机发令时刻 PC 已知。

三路信号中同一事件的时刻互相比对，得到全部两两偏移；闭环残差（三个偏移之和应为零）即系统对齐精度的直接度量。

### 6.3 标定流程建议

1. 实验前执行一轮标定（≥10 次重复事件，取中位数，记录散布）；
2. 长时间实验在中途与结束各补一轮，检验偏移稳定性与漂移；
3. 散布（如 IQR）即为该通路对齐不确定度的实测值，写入数据集元信息。

---

## 7. 实施要点清单

**采集端**

- [ ] 所有打戳统一调用 QPC / `steady_clock`，禁止混用 `system_clock`；
- [ ] 启动时记录一次 (walltime, steady_clock) 锚点，写入会话元数据；
- [ ] 串口读线程提升优先级，读到即打戳，避免在解析后打戳；
- [ ] IMU 包记录 (t_imu, t_arrival) 二元组，含包序号用于丢包检测；
- [ ] 相机迁移前先跑驱动能力探针，判定 `MFSampleExtension_DeviceTimestamp` 是否可用，再决定后端；
- [ ] MF 路径只读 DeviceTimestamp（QPC 域），勿用 GetSampleTime；FFmpeg 路径禁用 `use_wallclock_as_timestamps`，保留设备时间戳；MM 路径查 metadata 是否有相机硬件戳；
- [ ] 电机发令前后各打一次 QPC。

**离线处理端**

- [ ] IMU、相机各自做下包络线性拟合映射至主轴（滑窗在线更新可选）；
- [ ] 应用联合标定测得的固定偏移；
- [ ] 输出统一主轴时间戳 + walltime 锚点换算的绝对时刻。

**验证端**

- [ ] PTS 间隔直方图 + PTS−QPC 漂移曲线（部署前）；
- [ ] 联合标定闭环残差（每次实验）；
- [ ] 长时段拟合斜率监控（温漂预警）。

---

## 8. 预期精度与上限

| 误差源 | 处理后量级 |
|--------|-----------|
| 主时钟（QPC）分辨率 | < 1 μs，可忽略 |
| IMU 流内间隔（晶振插值后） | 晶振短稳，~μs 级 |
| IMU→主轴映射残差（下包络拟合） | 亚毫秒 |
| 相机 PTS 映射残差（驱动时间戳良好时） | ~1 ms |
| 曝光中心偏移标定残差 | 亚毫秒至 1 ms（取决于帧率与插值） |
| 电机执行延迟抖动 | 取决于控制器固件，需实测 |

**系统整体预期：1–2 ms 级跨模态对齐精度。** 这是无硬件同步线条件下的合理上限；若未来需要亚毫秒级（如高速视觉-惯性紧耦合），应更换支持外触发的相机与带 sync 引脚的 IMU，转为硬件触发架构。

---

## 附录：常见错误模式

1. **以 walltime 做对齐运算** → NTP 跳变导致时间倒退、间隔为负。
2. **直接使用串口到达时间作为 IMU 采样时刻** → 1–16 ms 锯齿抖动注入姿态解算。
3. **对 (t_imu, t_arrival) 做普通最小二乘** → 抖动单向偏置拉偏截距，应使用下包络。
4. **将相机时间戳当作曝光时刻** → 引入曝光/2 + 读出 + 传输的系统性偏移（可达十余 ms）。
5. **常数偏移代替线性映射** → 晶振频偏在小时级实验中累积上百 ms。
6. **只标定一次且不验证闭环残差** → 偏移漂移无从察觉。
7. **信任 MM 的 ElapsedTime-ms 作为曝光时刻** → 该字段官方声明仅 ±20 ms（末端 Java 层打戳），高帧率下即差数帧。
8. **在 MF 中误用 GetSampleTime() 做对齐** → 它是相对流起点的 presentation time，非采集绝对时刻；应读 `MFSampleExtension_DeviceTimestamp`（QPC 域）。
9. **假定换后端即提升精度而先重写框架** → 精度由驱动是否暴露硬件戳决定；应先跑驱动能力探针再决定是否迁移。
