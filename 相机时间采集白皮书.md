# 相机时间采集白皮书

**主题：Windows 平台 USB/UVC 相机的帧时间戳获取、对齐与失效防护**

版本：v1.0 · 日期：2026-06-14

---

## 0. 摘要

相机是多传感器系统中时间戳语义最复杂、最易被误用的通路。本白皮书系统阐述：相机帧时间戳在采集流水线中的来源与质量、各采集后端（Micro-Manager / FFmpeg-dshow / Media Foundation）的字段语义与陷阱、在无硬件时间戳条件下如何用传感器晶振栅格 + 物理锚点逼近亚帧精度、以及一个极易被忽视却足以毁掉全部对齐的失效模式——**丢帧静默错位**。

三条核心结论先行：

1. **更换采集后端本身不提升精度**，它只改变打戳点在流水线中的位置和你能读到的字段。精度由相机驱动是否暴露硬件时间戳决定。
2. **无硬件戳时，相机自身能提供的一切都退化为"一条由传感器晶振定义的均匀帧栅格"**。FFmpeg PTS 与手动帧号合成在信息量上等价；缺失的"与主时钟的绝对对齐"只能由一次外部物理测量（LED 标定）注入。
3. **丢帧是相机通路的头号隐蔽杀手**。若时间戳按帧计数器纯递推、或拟合横轴误用"收到的帧序号"，丢帧会导致此后所有帧永久错位且不留痕迹。必须主动检测。

---

## 1. 帧时间戳的流水线来源

一帧图像从光子到应用拿到时间戳，经历如下链路。每一环都可能是某后端的打戳点，越靠左越接近真实采集时刻、抖动越小：

```
曝光 → 传感器读出 → USB等时传输 → 内核UVC驱动 → 用户态采集层缓冲 → 应用回调/取帧
  │       │                        │                  │              │
曝光中点  读出完成                  驱动设备戳(最佳)    MMCore环形缓冲   软件到达戳(最差)
(真值)                          DeviceTimestamp/UVC PTS              ElapsedTime/wallclock
```

关键认识：**任何后端给出的时间戳，本质都是这条链路上某一点的时刻，没有任何一个直接等于"曝光中点"这个物理真值。** 曝光中点与各打戳点之间隔着曝光时间/2 + 读出 + 传输等固定偏移（详见第 6 节）。

---

## 2. 采集后端的字段语义

### 2.1 Micro-Manager

`ElapsedTime-ms` 是曝光时刻的代理量，但打戳点位于链路**最末端**——图像穿过 MMCore 环形缓冲、被 Java 层从 Core 取出之后才打戳。官方声明其精度不应假定优于约 **±20 ms**；`Time` 字段是 Java 层收到图像的时刻，与实际采集存在显著可变延迟且总是滞后。

工程后果：相机跑 100 Hz（10 ms/帧）时，±20 ms 即 ±2 帧的不确定度——这是"IMU 对齐后视频差几帧"现象的直接来源，而非 IMU 出错。

**例外（科研相机）**：Photometrics/Teledyne、Hamamatsu、Andor 等开启 `MetadataEnabled` 后，metadata 中有相机板载处理器生成的硬件时间戳（如 PVCAM 的 `PVCAM-FMD-TimestampBofNs`/`EofNs`）。这是绕过缓冲与 Java 层的最精确戳，有则务必弃用 ElapsedTime 改用之。

### 2.2 Media Foundation

MF 有两个极易混淆的字段，选错前功尽弃：

| 字段 | 含义 | 用途 |
|------|------|------|
| `IMFSample::GetSampleTime()` | **presentation time**，相对流起点的排程时间轴，100ns 单位 | 渲染排程用，**不可用于跨模态对齐**；驱动无设备戳时由系统时钟合成 |
| `MFSampleExtension_DeviceTimestamp`（`GetUINT64` 读取） | 设备侧时间戳，位于 **MFTIME 域，与 QPC 共享 epoch**，100ns 单位 | **目标字段**：直接落在 QPC 域、与 `steady_clock` 同源、零点无需换算 |

若 DeviceTimestamp 可用，相机与 IMU 天然同处一根尺子，省去整层映射。但大量相机驱动不填此字段，读取返回 `MF_E_ATTRIBUTENOTFOUND`（社区反复报告 `OnReadSample` 的 `llTimestamp` 与 `GetSampleTime()` 均非硬件 PTS）。

### 2.3 FFmpeg / dshow

dshow 的 `IMediaSample::GetTime` 由 capture filter 基于 DirectShow 参考时钟（底层 QPC）在帧进入 filter 时打戳，与 MF **走同一内核 UVC 驱动**——驱动不暴露硬件 PTS 时，dshow 与 MF 谁都变不出来。UVC 协议层有 `dwPresentationTime` 与 source clock reference，能否透传到用户态取决于相机固件是否填写、驱动是否转译，许多消费级 webcam 固件根本不填。

FFmpeg 配置要点：保留默认 `use_video_device_timestamps=true`；**禁用 `use_wallclock_as_timestamps 1`**（demuxer 读包时刻、经 rtbuffer 排队，最差路径）。注意 FFmpeg 输出的 PTS 常被换算为"启动瞬间 walltime + QPC 增量"的混合值，增量可信、零点带一次 walltime 误差。

### 2.4 后端选型小结

| 场景 | 选择 |
|------|------|
| 驱动提供 DeviceTimestamp/硬件戳 | 任意后端皆可，优选 MF（直接 QPC 域）；这是质的飞跃 |
| 驱动不提供，需自写采集 | MF 优于 dshow（API 现代、异步回调干净） |
| 已有 FFmpeg 管线且无硬件戳 | 保留即可，PTS 与自写采集信息量等价（见第 4 节） |
| 科研相机但用 MM | 查 metadata 硬件戳，优先于 ElapsedTime |

---

## 3. 驱动能力探针（任何迁移决策的前提）

**不要在判定驱动能力前重写采集框架。** 先写最小探针（MF SourceReader 异步回调即可），每帧同时记录并打印三个量：

1. `GetSampleTime()`——presentation time（相对流起点）
2. `GetUINT64(MFSampleExtension_DeviceTimestamp, ...)` 的 HRESULT 与值
3. 回调内立即取的 QPC（`QueryPerformanceCounter`）

判定与决策：

| 探针结果 | 含义 | 决策 |
|----------|------|------|
| DeviceTimestamp 可用，与回调 QPC 差为稳定小正数（几 ms） | 驱动提供 QPC 域采集戳 | **大赚**：直接用，相机与 IMU 零映射对齐 |
| 返回 `MF_E_ATTRIBUTENOTFOUND` | 驱动不给硬件戳 | 进入第 4 节"无硬件戳分支" |

---

## 4. 无硬件时间戳分支：晶振栅格 + 物理锚点

### 4.1 唯一信息源：传感器晶振定义的均匀帧栅格

固定帧率模式下，每帧由传感器自身晶振等间隔触发。该晶振与 IMU 晶振同类，短期稳定性远优于软件到达戳的逐帧抖动。因此相机本地时钟可表为 `t_cam_local = 帧号 × 标称周期`。

**重要等价性**：FFmpeg 的 PTS（设备时间戳模式）与手动 `帧号 × 周期` 在信息量上等价——两者都源于同一传感器晶振、都无独立物理时刻注入。**自己数帧号不会比 FFmpeg PTS 更优**，FFmpeg 的帧号还在更靠近驱动的层级数，少一层用户态延迟。故无硬件戳时不必另起炉灶，沿用 PTS 即可。

### 4.2 必要前提：锁死曝光与帧率

晶振栅格的"等间隔"假设极其脆弱，以下设置会摧毁它，必须全部关闭/锁定：

- **关闭自动曝光、自动增益**：自动曝光为凑亮度动态改变曝光时间，帧间隔随之浮动，栅格假设破裂、拟合失效。**锁死曝光时间**。
- **强制 constant/fixed frame rate**，禁用 variable frame rate。
- **优先 YUY2/raw 而非 MJPEG**：MJPEG 编码耗时随画面复杂度变化，给到达戳注入可变延迟。

### 4.3 流内映射：下包络线性拟合

将相机本地栅格映射到主轴（QPC）：`t_master = a · 帧号 + b`，a 吸收晶振频偏，b 吸收固定延迟。

拟合方法用**下包络拟合**（lower-envelope）：到达抖动只单向增大延迟，故每个时间窗内延迟最小的点最接近真实下界。算法：分窗 → 每窗取 `到达QPC − 帧号×周期` 最小点 → 对下包络点线性回归（可加 RANSAC）→ 长时段滑窗在线更新 a/b 跟踪温漂。

> **致命陷阱（见第 5 节）**：拟合横轴的"帧号"绝不能用"收到的第几帧"这个连续计数器，否则丢帧会被直线骨架填平、错位无法察觉。必须用真实帧号或真实到达时刻。

### 4.4 绝对锚点：LED 标定（无硬件戳时不可省）

栅格拟合解决了"帧间相对精度"，但解决不了两件事：相机本地时钟的**绝对零点**（b 中混着未知固定延迟）与曝光中心偏移。这两个固定量由一次 LED 闪光标定一并吃掉：

PC 用 QPC 已知时刻点亮 LED → 相机拍到亮起帧（前后帧亮度插值取亚帧精度）→ `主时钟触发时刻` 与 `拟合后该帧主轴时刻` 之差 = 须减去的总固定偏移。

**性质澄清**：LED 标定不是"第二种合成时间戳"，它是**唯一的物理输入**。PTS/帧号给"帧间多准"（相对），LED 给"这套帧坐在主轴哪个位置"（绝对），两者正交、不冗余。"PTS 和帧号合成没区别"成立于相对栅格内部，但不能外推到绝对锚点——后者相机自身永远给不了。

### 4.5 精度三档（信息量严格递增）

| 档位 | 内容 | 补的是什么 | 成本 |
|------|------|-----------|------|
| 1. FFmpeg PTS（=帧号合成） | 晶振栅格，零物理锚点 | —（起点） | 零 |
| 2. + LED 标定 | 晶振栅格 + 一次物理锚点 | 绝对位置 | 低（一个 LED + 串口） |
| 3. 光学时间码 | 逐帧物理测量 | 不再依赖晶振假设 | 中（LED 阵列 + 解码器） |

落在哪一档取决于需求：仅需相机流内相对时序或事后互相关可找回偏移 → 档 1 够用；需相机帧与 IMU 采样的确定绝对对应（不依赖事后互相关）→ 档 2；需逐帧独立物理时刻、且要自纠晶振漂移与丢帧 → 档 3。

---

## 5. 失效模式专题：丢帧静默错位

这是相机通路最隐蔽、破坏力最大的失效模式，单列防护。

### 5.1 危险机理

若 PTS 按帧计数器纯递推（`pts = 收到的第N帧 × 周期`）：相机吞掉第 100 帧时，FFmpeg 把实际的第 101 帧仍编号为 100，标成 100×周期。结果——**这一帧及其后所有帧的时间戳永久前移一个帧周期，无声无痕**。视频整体比真实晚一帧，IMU 对齐处处差一帧，却无任何报错。

### 5.2 转机：设备时间戳 PTS 不是纯递推

设备时间戳模式（`use_video_device_timestamps=true`）下，PTS 是驱动/参考时钟在每帧到达时**实际读取的时刻**，而非计数器递推。丢一帧时，下一帧到达时刻自然比前帧大约**两个帧周期**——丢帧在 PTS 序列里留下"双倍间隔"的洞，**可被发现**。这正是"FFmpeg 合成 PTS 并非简单虚构"的确切含义：它带着驱动侧实际到达时刻，但仍是软件层、带几 ms 抖动，"留痕"不等于"精确"。

### 5.3 拟合会抹平痕迹——必须警惕

第 4.3 的下包络拟合若用"收到的帧序号"作横轴，会把丢帧的洞**填平**——因为强行假设了帧号连续均匀，错位重新出现。**拟合横轴必须用带洞的真实量，否则拟合反成帮凶。**

### 5.4 丢帧检测方案（按优先级）

1. **读相机硬件帧计数器**：许多 UVC 相机 metadata 有硬件帧号或 UVC SCR/PTS。丢帧时该号**跳变**（98→100，缺 99）。用真实帧号作拟合横轴，洞保留，丢帧一眼可见。最干净。
2. **监控 PTS 相邻差值**：逐帧算 `Δpts = pts[n] − pts[n−1]` 画直方图。正常 33ms 单峰；出现 66ms/99ms 次峰即丢帧，可数出丢几帧。标记位置后按真实时刻布点拟合，洞不被填平。（此步本就在质量验证清单里，此处多一重"丢帧探测器"身份。）
3. **独立帧计数交叉校验**：回调里自维护"收到第几帧"计数器，与方法 1 的硬件帧号比对，发散点即丢帧点。无硬件帧号时以方法 2 的 Δpts 异常代替。
4. **源头降丢帧**：增大驱动 rtbuffer 队列深度、采集线程提优先级、相机独占 USB root hub、采集线程不做重活。最佳处理永远是不丢。

---

## 6. 曝光中心偏移

无论时间戳来源，其语义均为帧的采集/读出/到达时刻之一，**均非曝光中心**。与曝光中心间隔：曝光时间/2 + 传感器读出时间 + USB 等时传输时间（USB 2.0 上一帧 MJPEG 传输本身需数毫秒至十余毫秒）。

该项为"稳定固定偏移 + 小抖动"，由 LED 标定（第 4.4 节）与绝对锚点一并测出。**若实验中曝光时间动态变化（如未锁自动曝光），此偏移随之变化**——这是必须锁曝光的又一理由；确需变曝光则按曝光设置分段标定。

---

## 7. 采集实现清单

**配置**
- [ ] 关闭自动曝光、自动增益，锁死曝光时间
- [ ] 强制固定帧率，禁用 variable frame rate
- [ ] 优先 YUY2/raw，避免 MJPEG 的可变编码延迟
- [ ] 相机独占 USB root hub，避免总线带宽竞争

**打戳**
- [ ] 迁移前先跑驱动能力探针（第 3 节），判定 DeviceTimestamp 可用性
- [ ] MF 路径只读 DeviceTimestamp，勿用 GetSampleTime
- [ ] FFmpeg 路径禁用 `use_wallclock_as_timestamps`，保留设备时间戳
- [ ] 回调第一行即取 QPC，在任何 memcpy/解码/格式转换之前
- [ ] 采集线程提优先级，重活（写盘/编码）丢到其他线程

**对齐**
- [ ] 有硬件戳：直接用 DeviceTimestamp/相机硬件戳作采集时刻
- [ ] 无硬件戳：帧栅格下包络拟合至主轴 + LED 标定固定偏移
- [ ] 拟合横轴用真实帧号/真实到达时刻，**绝不用"收到的帧序号"**

**验证与防护**
- [ ] 时间戳间隔直方图（应为帧周期单峰，锯齿/弥散即软件补打）
- [ ] 时间戳−QPC 漂移曲线（斜率非零须线性映射而非常数偏移）
- [ ] Δpts 直方图监控丢帧（双倍间隔次峰）
- [ ] 硬件帧号与独立计数交叉校验
- [ ] LED 标定闭环残差作为相机通路对齐精度的实测值

---

## 8. 决策流程图

```
开始
 │
 ├─ 跑驱动能力探针 ──► DeviceTimestamp 可用？
 │                          │
 │              ┌───────────┴───────────┐
 │             是                       否
 │              │                       │
 │     直接用(QPC域采集戳)        进入无硬件戳分支
 │     仅需曝光偏移标定                  │
 │              │              锁曝光+锁帧率
 │              │                       │
 │              │              帧栅格(PTS/帧号)下包络拟合
 │              │                       │
 │              │              需要绝对对应? ──否──► 档1收工(仅相对)
 │              │                       │是
 │              │                  LED标定(档2)
 │              │                       │
 │              │              需逐帧物理时刻/自纠丢帧漂移? ──否──► 档2收工
 │              │                       │是
 │              │                  光学时间码(档3)
 │              │                       │
 └──────────────┴───────────────────────┘
                       │
              全程: Δpts监控丢帧 + 拟合用真实帧号
```

---

## 附录：常见错误模式

1. **信任 MM ElapsedTime-ms 作为曝光时刻** → 官方仅 ±20 ms（末端 Java 打戳），高帧率下差数帧。
2. **MF 中误用 GetSampleTime() 对齐** → 它是相对流起点的 presentation time；应读 `MFSampleExtension_DeviceTimestamp`（QPC 域）。
3. **假定换后端即提升精度而先重写框架** → 精度由驱动是否给硬件戳决定；先探针后迁移。
4. **未锁自动曝光做帧栅格拟合** → 帧间隔浮动，等间隔假设破裂。
5. **把 LED 标定当作可省的冗余步骤** → 它是无硬件戳时唯一的绝对物理锚点，与相对栅格正交。
6. **将相机时间戳当曝光中点** → 漏掉曝光/2 + 读出 + 传输的系统偏移（可达十余 ms）。
7. **拟合横轴用"收到的帧序号"** → 丢帧的洞被直线填平，错位永久且无痕。
8. **纯递推 PTS 且不监控 Δpts** → 丢帧后全程静默前移一帧。
